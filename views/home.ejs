<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Investment Platform</title>
    <!-- Tailwind CSS served locally -->
    <link href="./home.css" rel="stylesheet">
    <link rel="shortcut icon" href="/logo.svg" type="image/x-icon">
    <style>
        @keyframes marquee {
            0% {
                transform: translateX(100%);
            }

            100% {
                transform: translateX(-100%);
            }
        }

        .marquee {
            animation: marquee 20s linear infinite;
        }

        .slide {
            transition: opacity 0.5s ease-in-out;
        }
    </style>
</head>

<body class="pt-16"> <!-- Added padding-top for fixed header -->
    <!-- Header Start -->
    <header class="fixed w-full top-0 z-50 bg-white shadow-md">
        <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo -->
                <div class="flex-shrink-0">
                    <a href="#" class="flex items-center">
                        <img class="h-16 w-auto" src="/logo.svg" alt="Logo">
                        <span class="ml-2 text-xl font-bold text-blue-800">FOURx</span>
                    </a>
                </div>

                <!-- Desktop Navigation -->
                <div class="hidden md:flex items-center space-x-8">
                    <a href="#" id="homebtn" class="text-gray-600 hover:text-blue-600 transition duration-300">Home</a>
                    <a href="#" id="historybtn"
                        class="text-gray-600 hover:text-blue-600 transition duration-300">History</a>
                    <a href="#" id="aboutbtn"
                        class="text-gray-600 hover:text-blue-600 transition duration-300">About</a>
                    <a href="#" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                        Log out
                    </a>
                </div>

                <!-- Mobile Menu Button -->
                <div class="md:hidden">
                    <button id="menu-btn" class="text-gray-600 hover:text-blue-600 focus:outline-none"
                        onclick="toggleMenu()">
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M4 6h16M4 12h16m-7 6h7" />
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Mobile Menu -->
            <div id="mobile-menu" class="hidden md:hidden bg-white shadow-lg">
                <a href="" id="homebtn333" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">Home</a>
                <a href="" id="historybtn333" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">History</a>
                <a href="" id="aboutbtn333" class="block px-4 py-2 text-gray-600 hover:bg-gray-100">About</a>
                <a href="" class="block px-4 py-2 text-red-600 hover:bg-gray-100">Log out</a>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Banner Slider -->
        <div class="h-[200px] md:h-[300px] mt-6 rounded-xl overflow-hidden relative">
            <div class="slide active absolute inset-0 bg-cover bg-center opacity-100"
                style="background-image: url('/image1.svg')"></div>
            <div class="slide absolute inset-0 bg-cover bg-center opacity-0"
                style="background-image: url('/image2.svg')"></div>
            <div class="slide absolute inset-0 bg-cover bg-center opacity-0"
                style="background-image: url('/image3.svg')"></div>
            <div class="slide absolute inset-0 bg-cover bg-center opacity-0"
                style="background-image: url('/image4.svg')"></div>
        </div>

        <!-- Announcements -->
        <div class="bg-blue-800 text-white py-4 mt-4 rounded-lg overflow-hidden">
            <div class="marquee whitespace-nowrap w-full">
                <span class="inline-block pr-8">
                    🎉 New users get 15% bonus on first investment! • 💼 Refer friends and earn 10% commission • 📈 24/7
                    Market Updates
                </span>
            </div>
        </div>

        <!-- Compact Single Row Action Buttons -->
        <div class="flex flex-nowrap justify-center items-center gap-2 p-6 overflow-x-auto w-full">
            <!-- Deposit Button -->
            <button id="depositbtn"
                class="bg-blue-800 text-white px-3 py-2 text-sm rounded-md hover:bg-blue-500 flex items-center justify-center gap-1.5 transition-all duration-200 shadow-sm hover:shadow whitespace-nowrap flex-shrink-0">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round"
                        d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
                Deposit
            </button>

            <!-- Withdraw Button -->
            <button id="widrawbtn"
                class="bg-blue-800 text-white px-3 py-2 text-sm rounded-md hover:bg-blue-500 flex items-center justify-center gap-1.5 transition-all duration-200 shadow-sm hover:shadow whitespace-nowrap flex-shrink-0">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round"
                        d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />
                </svg>
                Withdraw
            </button>

            <!-- Referral Button -->
            <button id="refferalbtn"
                class="bg-blue-800 text-white px-3 py-2 text-sm rounded-md hover:bg-blue-500 flex items-center justify-center gap-1.5 transition-all duration-200 shadow-sm hover:shadow whitespace-nowrap flex-shrink-0">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round"
                        d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z" />
                </svg>
                Referral
            </button>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 pb-20">
            <% tickets.forEach((ticket, index)=> { %>
                <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:-translate-y-1 transition-transform cursor-pointer ticket-item"
                    data-ticket-id="<%= ticket.id %>">
                    <img src="<%= ticket.image %>" alt="<%= ticket.name %>" class="w-full h-48 object-cover">
                    <div class="p-4">
                        <h3 class="text-lg text-blue-800 font-bold mb-2">
                            <%= ticket.name %>
                        </h3>

                        <% if (!ticket.isCustomAmount) { %>
                            <p class="text-lg text-blue-800 font-semibold mb-3">Value: <%= ticket.amount %>
                                    <%= ticket.currency || 'PKR' %>
                            </p>
                            <p class="text-lg text-blue-800 font-semibold mb-3">Profit: <%= Math.round(ticket.profit) %>
                                    <%= ticket.currency || 'PKR' %>
                            </p>
                            <% } else { %>
                                <p class="text-lg text-blue-800 font-semibold mb-3">Custom Amount: <%=
                                        ticket.minCustomAmount %>+ <%= ticket.currency || 'PKR' %>
                                </p>
                                <p class="text-lg text-blue-800 font-semibold mb-3">Profit: <%= ticket.profitPercentage
                                        %>% Daily</p>
                                <% } %>

                                    <div class="flex justify-between items-center">
                                        <p class="text-lg text-blue-800 font-semibold mb-3">Validity: <%=
                                                ticket.validityHours %> Hours</p>
                                        <button
                                            class="bg-blue-600 text-white px-3 py-1.5 text-sm rounded-lg hover:bg-blue-700 transition-colors">
                                            Invest Now
                                        </button>
                                    </div>
                    </div>
                </div>
                <% }); %>
        </div>

        <!-- Projects Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 pb-20">
            <!-- Project Cards (Reduced to 4 for better mobile view) -->
            <!-- Repeat this card structure as needed -->
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:-translate-y-1 transition-transform">
                <img src="project1.svg" alt="Project" class="w-full h-48 object-cover">
                <div class="p-4">
                    <h3 class="text-lg text-blue-800 font-bold mb-2">Renewable Energy Crowdfunding (Solar/Wind Farms)
                    </h3>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Value: 1500 PKR </p>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Profit: 33 PKR</p>
                    <div class="flex justify-between items-center">
                        <p class="text-lg text-blue-800 font-semibold mb-3">Validity: 24 Hours</p>
                        <button
                            class="bg-blue-600 text-white px-3 py-1.5 text-sm rounded-lg hover:bg-blue-700 transition-colors">
                            Invest Now
                        </button>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:-translate-y-1 transition-transform">
                <img src="project2.svg" alt="Project" class="w-full h-48 object-cover">
                <div class="p-4">
                    <h3 class="text-lg text-blue-800 font-bold mb-2">AI-Powered Automated Trading Fund</h3>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Value: 3000 PKR </p>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Profit: 70 PKR</p>
                    <div class="flex justify-between items-center">
                        <p class="text-lg text-blue-800 font-semibold mb-3">Validity: 24 Hours</p>
                        <button
                            class="bg-blue-600 text-white px-3 py-1.5 text-sm rounded-lg hover:bg-blue-700 transition-colors">
                            Invest Now
                        </button>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:-translate-y-1 transition-transform">
                <img src="project3.svg" alt="Project" class="w-full h-48 object-cover">
                <div class="p-4">
                    <h3 class="text-lg text-blue-800 font-bold mb-2">Global Real Estate Crowdfunding</h3>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Value: 5000 PKR </p>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Profit: 111 PKR</p>
                    <div class="flex justify-between items-center">
                        <p class="text-lg text-blue-800 font-semibold mb-3">Validity: 24 Hours</p>
                        <button
                            class="bg-blue-600 text-white px-3 py-1.5 text-sm rounded-lg hover:bg-blue-700 transition-colors">
                            Invest Now
                        </button>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:-translate-y-1 transition-transform">
                <img src="project4.svg" alt="Project" class="w-full h-48 object-cover">
                <div class="p-4">
                    <h3 class="text-lg text-blue-800 font-bold mb-2">SaaS for Emerging Markets</h3>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Value: 10,000 PKR </p>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Profit: 250 PKR</p>
                    <div class="flex justify-between items-center">
                        <p class="text-lg text-blue-800 font-semibold mb-3">Validity: 24 Hours</p>
                        <button
                            class="bg-blue-600 text-white px-3 py-1.5 text-sm rounded-lg hover:bg-blue-700 transition-colors">
                            Invest Now
                        </button>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:-translate-y-1 transition-transform">
                <img src="project5.svg" alt="Project" class="w-full h-48 object-cover">
                <div class="p-4">
                    <h3 class="text-lg text-blue-800 font-bold mb-2">Luxury & Rare Asset Investment (Watches,Cryprto,
                        NFTs)</h3>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Value: 10,000+ PKR </p>
                    <p class="text-lg text-blue-800 font-semibold mb-3">Profit: 4%</p>
                    <div class="flex justify-between items-center">
                        <p class="text-lg text-blue-800 font-semibold mb-3">Validity: 24 Hours</p>
                        <button
                            class="bg-blue-600 text-white px-3 py-1.5 text-sm rounded-lg hover:bg-blue-700 transition-colors">
                            Invest Now
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer Navigation with SVG icons -->
    <nav class="fixed bottom-0 w-full bg-white shadow-[0_-2px_10px_rgba(0,0,0,0.1)] flex justify-around py-3 z-50">
        <a href="#" id="homebtn" class="flex flex-col items-center text-blue-700 px-2">
            <svg class="h-6 w-6 text-blue-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
            <span class="text-xs md:text-sm">Home</span>
        </a>
        <a href="#" id="assetsbtn" class="flex flex-col items-center text-gray-500 hover:text-blue-600 px-2">
            <svg class="h-6 w-6 text-blue-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
            <span class="text-xs md:text-sm">Assets</span>
        </a>
        <a href="#" id="teambtn2" class="flex flex-col items-center text-gray-500 hover:text-blue-600 px-2">
            <svg class="h-6 w-6 text-blue-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
            </svg>
            <span class="text-xs md:text-sm">Team</span>
        </a>
        <a href="#" id="profilebtn"
            class="flex flex-col items-center text-gray-500 hover:text-blue-600 px-2 transition-colors duration-200 group"
            aria-label="User profile">
            <svg class="h-6 w-6 text-blue-700 group-hover:text-blue-600" fill="none" viewBox="0 0 24 24"
                stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
            <span class="text-xs md:text-sm group-hover:text-blue-600">Profile</span>
        </a>
    </nav>

    <script>
        // Mobile Menu Toggle
        function toggleMenu() {
            const mobileMenu = document.getElementById('mobile-menu');
            const menuBtn = document.getElementById('menu-btn');
            mobileMenu.classList.toggle('hidden');
            menuBtn.classList.toggle('text-blue-600');
        }

        // Add event listener for menu button
        document.getElementById('menu-btn').addEventListener('click', function (e) {
            e.preventDefault();
            toggleMenu();
        });

        // Close mobile menu when clicking outside
        document.addEventListener('click', function (event) {
            const mobileMenu = document.getElementById('mobile-menu');
            const menuBtn = document.getElementById('menu-btn');

            if (!mobileMenu.contains(event.target) && !menuBtn.contains(event.target)) {
                mobileMenu.classList.add('hidden');
                menuBtn.classList.remove('text-blue-600');
            }
        });

        // Slideshow Functionality
        let currentSlide = 0;
        const slides = document.querySelectorAll('.slide');
        const totalSlides = slides.length;

        function showSlide(index) {
            // Handle negative indexes
            if (index < 0) index = totalSlides - 1;
            // Handle overflow indexes
            index = index % totalSlides;

            slides.forEach((slide, i) => {
                slide.style.opacity = i === index ? '1' : '0';
                slide.classList.toggle('active', i === index);
            });
            currentSlide = index;
        }

        function nextSlide() {
            showSlide(currentSlide + 1);
        }

        // Initialize slideshow
        let slideInterval = setInterval(nextSlide, 5000);
        showSlide(0);

        // Pause slideshow on hover
        const banner = document.querySelector('.slide');
        if (banner) {
            banner.parentElement.addEventListener('mouseenter', () => clearInterval(slideInterval));
            banner.parentElement.addEventListener('mouseleave', () => {
                slideInterval = setInterval(nextSlide, 5000);
            });
        }

        document.getElementById("depositbtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/deposit'; // Redirect
        });
        document.getElementById("homebtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/home'; // Redirect
        });
        document.getElementById("historybtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/history'; // Redirect
        });
        document.getElementById("aboutbtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/about'; // Redirect
        });
        document.getElementById("aboutbtn333").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/about'; // Redirect
        });

        document.getElementById("widrawbtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/withdraw'; // Redirect
        });

        document.getElementById("refferalbtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/team'; // Redirect
        });

        document.getElementById("teambtn2").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/team'; // Redirect
        });

        document.getElementById("assetsbtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/assets'; // Redirect
        });

        document.getElementById("profilebtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/profile'; // Redirect
        });

        document.getElementById("homebtn").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/home'; // Redirect
        });




        document.getElementById("homebtn333").addEventListener("click", function (e) {
            e.preventDefault(); // Prevent default navigation
            window.location.href = '/home'; // Redirect
        });


        // Attach logout function to all logout buttons
        document.querySelectorAll('.logout-btn').forEach(button => {
            button.addEventListener('click', function (event) {
                event.preventDefault(); // Prevent default link behavior
                logout();
            });
        });

        async function logout() {
            try {
                const response = await fetch('/api/auth/logout', {  // Updated endpoint
                    method: 'POST',
                    credentials: 'include',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                });

                if (!response.ok) {
                    throw new Error('Logout failed');
                }

                window.location.href = '/login';
            } catch (error) {
                console.error('Logout error:', error);
                alert('Could not log out. Please try again.');
            }
        }

        document.querySelectorAll('.ticket-item').forEach(ticket => {
            ticket.addEventListener('click', function () {
                const ticketId = this.getAttribute('data-ticket-id');
                window.location.href = `/assets?ticket=${ticketId}`;
            });
        });
    </script>
</body>

</html>